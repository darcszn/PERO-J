{
  "name": "pero-j-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for the PERO-J pipeline: contract → indexer → API → frontend",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "deploy": "node helpers/deploy.js",
    "install-playwright": "playwright install chromium"
  },
  "dependencies": {
    "@playwright/test": "^1.45.0",
    "@stellar/stellar-sdk": "12.3.0"
  }
}
